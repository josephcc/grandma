---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PageHero from '../../components/PageHero.astro';
import DonationInfo from '../../components/DonationInfo.astro';
import { getEntry, render } from 'astro:content';

const entry = await getEntry('pages', 'tw/donate');
if (!entry) throw new Error('Content not found: tw/donate');
const { Content } = await render(entry);
---

<BaseLayout title={entry.data.title} description={entry.data.description} lang="zh-TW" locale="tw">
  <PageHero title="愛心捐款" subtitle="Donate" />
  <article class="max-w-3xl mx-auto px-4 sm:px-6 py-16 md:py-24">
    <div class="prose prose-lg prose-gray max-w-none prose-headings:text-brand-teal prose-headings:font-sans prose-a:text-brand-teal prose-p:text-gray-500 prose-h1:hidden reveal">
      <Content />
    </div>
    <div class="reveal">
      <DonationInfo locale="tw" />
    </div>
  </article>
</BaseLayout>
